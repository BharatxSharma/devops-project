services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    ports:
env_file:
  - .env.prod  - "${PORT}:${PORT}"




  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
